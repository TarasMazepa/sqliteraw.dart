name: sample_flutter
description: Sample Flutter app using sqliteraw
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.2.0
  flutter:
    sdk: flutter
  sqliteraw:
    path: '../../sqliteraw'
  sqliteraw_sample_dart:
    path: '../dart'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
