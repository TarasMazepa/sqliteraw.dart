name: sample_flutter
description: Sample Flutter app using sqliteraw
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4
  path: ^1.9.1
  path_provider: ^2.1.5
  sqliteraw:
    path: '../../sqliteraw'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
