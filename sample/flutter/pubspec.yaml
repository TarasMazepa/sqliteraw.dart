name: sample_flutter
description: Sample Flutter app using sqliteraw_dart
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  sqliteraw_dart:
    path: '../../sqliteraw'

dev_dependencies:
  ffi: ^2.1.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
